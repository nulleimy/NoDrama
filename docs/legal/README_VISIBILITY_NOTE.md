# Visibility Control Note

The connected repository file/commit interface can modify repository contents but does not expose the repository-settings operation required to change public/private visibility.

Therefore the legal/source baseline can be committed here, while the visibility switch remains a separate repository-administration action.

This note is operational evidence only and may be removed after the repository is confirmed private.
